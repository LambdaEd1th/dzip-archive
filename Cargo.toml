[package]
name = "dzip"
version.workspace = true
edition.workspace = true
repository.workspace = true
license.workspace = true
description = "Pure Rust library for reading, extracting, creating, and inspecting Dzip archives"
readme = "../../README.md"
keywords = ["archive", "compression", "dzip", "gamedev"]
categories = ["compression", "encoding"]

[dependencies]
byteorder = "1.5"
bzip = { version = "0.103.0", path = "../codecs/bzip", default-features = false, features = ["std"], optional = true }
dz = { version = "0.4.1", path = "../codecs/dz", optional = true }
lzma = { version = "0.920.0", path = "../codecs/lzma", features = ["decode"], optional = true }
rayon = { version = "1.12", optional = true }
serde = { workspace = true, optional = true }
zlib = { version = "0.113.0", path = "../codecs/zlib", default-features = false, features = ["std"], optional = true }

[features]
default = ["encode", "decode", "all-codecs"]
encode = []
decode = []
all-codecs = ["bzip", "dz", "lzma", "zlib"]
bzip = ["dep:bzip"]
dz = ["dep:dz"]
lzma = ["dep:lzma"]
zlib = ["dep:zlib"]
parallel = ["dep:rayon"]
serde = ["dep:serde"]

[dev-dependencies]
sha2 = "0.11"
