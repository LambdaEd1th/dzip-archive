[package]
name = "dzip"
version.workspace = true
edition.workspace = true
repository.workspace = true
license.workspace = true
description = "Pure Rust library for reading, extracting, creating, and inspecting Dzip archives"
readme = "../../README.md"
keywords = ["archive", "compression", "dzip", "gamedev"]
categories = ["compression", "encoding"]

[dependencies]
byteorder = "1.5"
bzip = { version = "0.4.1", path = "../codecs/bzip", optional = true }
dz = { version = "0.4.1", path = "../codecs/dz", default-features = false, optional = true }
lzma = { version = "0.4.1", path = "../codecs/lzma", optional = true }
rayon = { version = "1.12", optional = true }
serde = { workspace = true, optional = true }
zlib = { version = "0.4.1", path = "../codecs/zlib", optional = true }

[features]
default = ["encode", "decode", "all-codecs"]
encode = []
decode = []
all-codecs = ["bzip", "dz", "lzma", "zlib"]
bzip = ["dep:bzip"]
dz = ["dep:dz"]
lzma = ["dep:lzma"]
zlib = ["dep:zlib"]
parallel = ["dep:rayon"]
serde = ["dep:serde"]

[dev-dependencies]
sha2 = "0.11"
